"""Drop-in components for kirogate's rotation pool.

Each module is self-contained, imports only stdlib + httpx, and can be
copied into kirogate/ without other changes. See the README for the
6-line patch needed in server.py for each one.
"""
